# Set tab titles for a SpacetimeviewList using names

Set tab titles for a SpacetimeviewList using names

## Usage

``` r
# S3 method for class 'SpacetimeviewList'
names(x) <- value
```

## Arguments

- x:

  A SpacetimeviewList object

- value:

  Character vector of tab titles

## Value

The SpacetimeviewList with updated tab titles
